spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lostmatch3_dev?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

meetup:
  ml-service:
    base-url: ${ML_SERVICE_BASE_URL:http://127.0.0.1:8000}
    connect-timeout: ${ML_SERVICE_CONNECT_TIMEOUT:5s}
    read-timeout: ${ML_SERVICE_READ_TIMEOUT:120s}
